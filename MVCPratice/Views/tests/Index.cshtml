@model IEnumerable<MVCPratice.Models.test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <input type="button" value="新增表單" onclick="location.href='@Url.Action("Create")'">
    <!--@Html.ActionLink("Create New", "Create")-->
</p>
<!----><table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CDE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NAM)
        </th>
        <th></th>
    </tr>-->

<!----@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CDE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}-->

</table>
